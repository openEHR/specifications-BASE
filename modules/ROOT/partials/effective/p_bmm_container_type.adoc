[cols="2,3", frame=none, grid=rows]
|===
2+^h|*P_BMM_CONTAINER_TYPE*

2+a|Persistent form of `BMM_CONTAINER_TYPE`.

2+|Inherits: xref:{class_p_bmm_type}[P_BMM_TYPE]

2+h|*Attributes*

|*bmm_type*: `xref:{class_bmm_container_type}[BMM_CONTAINER_TYPE]` [0..1]
a|Result of `_create_bmm_type()_` call.

|*container_type*: `xref:{class_string}[String]` [1..1]
a|The type of the container. This converts to the `_root_type_` in `BMM_GENERIC_TYPE`. Persisted attribute.

|*type_def*: `xref:{class_p_bmm_base_type}[P_BMM_BASE_TYPE]` [0..1]
a|Type definition of `_type_`, if not a simple String type reference. Persisted attribute.

|*type*: `xref:{class_string}[String]` [0..1]
a|The target type; this converts to the first parameter in `_generic_parameters_` in `BMM_GENERIC_TYPE`. Persisted attribute.

2+h|*Functions*

|_(abstract)_ *create_bmm_type* ( +
a_schema: `xref:{class_bmm_model}[BMM_MODEL][1]`, +
a_class_def: `xref:{class_bmm_class}[BMM_CLASS][1]` +
): `void` [1..1]
a|Create appropriate `BMM_XXX` object; effected in descendants. +
_Inherited from xref:{class_p_bmm_type}[P_BMM_TYPE]_

|_(abstract)_ *as_type_string* (): `xref:{class_string}[String]` [1..1]
a|Formal name of the type for display. +
_Inherited from xref:{class_p_bmm_type}[P_BMM_TYPE]_

|*type_ref* (): `xref:{class_p_bmm_base_type}[P_BMM_BASE_TYPE]` [1..1]
a|The target type; this converts to the first parameter in `_generic_parameters_` in `BMM_GENERIC_TYPE`. Persisted attribute.
|===
