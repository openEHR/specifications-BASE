[cols="2,3", frame=none, grid=rows]
|===
2+^h|*BMM_MODEL_ACCESS*

2+a|Access to BMM models that have been loaded and validated from one or more schema sets.

2+|Inherits: xref:{class_bmm_definitions}[BMM_DEFINITIONS]

2+h|*Constants*

|*Bmm_internal_version*: `xref:{class_string}[String]{nbsp}={nbsp}` [1..1]
a|Current internal version of BMM meta-model, used to determine if a given schema can be processed by a given implementation of the model. +
_Inherited from xref:{class_bmm_definitions}[BMM_DEFINITIONS]_

|*Schema_name_delimiter*: `xref:{class_string}[String]{nbsp}={nbsp}"::"` [1..1]
a|Delimiter used to separate schema id from package path in a fully qualified path. +
_Inherited from xref:{class_bmm_definitions}[BMM_DEFINITIONS]_

|*Package_name_delimiter*: `xref:{class_string}[String]{nbsp}={nbsp}"."` [1..1]
a|Delimiter used to separate package names in a package path. +
_Inherited from xref:{class_bmm_definitions}[BMM_DEFINITIONS]_

|*Bmm_schema_file_extension*: `xref:{class_string}[String]{nbsp}={nbsp}".bmm"` [1..1]
a|Extension used for BMM files. +
_Inherited from xref:{class_bmm_definitions}[BMM_DEFINITIONS]_

2+h|*Attributes*

|*schema_directories*: `xref:{class_list}[List]<xref:{class_string}[String]>` [0..1]
a|List of directories where all the schemas loaded here are found.

|*all_schemas*: `xref:{class_list}[List]<xref:{class_string}[String]>` [0..1]
a|All schemas found and loaded from `_schema_directory_`. Keyed by `_schema_id_`.

|*bmm_models*: `xref:{class_hash}[Hash]<xref:{class_string}[String],xref:{class_bmm_model}[BMM_MODEL]>` [0..1]
a|Top-level (root) schemas in use. Table is keyed by `_model_id_`.

|*matching_bmm_models*: `xref:{class_list}[List]<xref:{class_string}[String]>` [0..1]
a|`BMM_MODELs` keyed by `_model_id()_` and any shorter form used in calls to

2+h|*Functions*

|*initialise_with_load_list* ( +
a_schema_dirs: `xref:{class_list}[List]<xref:{class_string}[String]>[1]`, +
a_schema_load_list: `xref:{class_list}[List]<xref:{class_string}[String]>` +
): `void` [1..1]
a|Initialise with a specific schema load list, usually a sub-set of schemas that will be found in a specified directories `_a_schema_dirs_`.

|*initialise_all* ( +
a_schema_dirs: `xref:{class_list}[List]<xref:{class_string}[String]>[1]` +
): `void` [1..1]
a|Load all schemas found in a specified directories `_a_schema_dirs_`.

|*reload_schemas* (): `void` [1..1]
a|Reload BMM schemas.

|*bmm_model* ( +
a_model_key: `xref:{class_string}[String][1]` +
): `xref:{class_bmm_model}[BMM_MODEL]` [1..1]
a|Return ref model containing the model key which is a `_model_id_` or any shorter form e.g. model id minus the version. If a shorter key is used, the `BMM_MODEL` with the highest version will be selected.

|*has_bmm_model* ( +
a_model_key: `xref:{class_string}[String][1]` +
): `xref:{class_boolean}[Boolean]` [1..1]
a|True if a model for a `_model_key_` is available. A model key is a `_model_id_` or any shorter form e.g. model id minus the version. If a shorter key is used, the Result s True if a `BMM_MODEL` with any version exists.
|===
