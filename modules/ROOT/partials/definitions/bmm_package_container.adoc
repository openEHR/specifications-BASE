[cols="^1,3,5"]
|===
h|*Class*
2+^h|*BMM_PACKAGE_CONTAINER*

h|*Description*
2+a|A BMM model component that contains packages and classes.

h|*Inherit*
2+|`xref:{class_bmm_model_element}[BMM_MODEL_ELEMENT]`

h|*Attributes*
^h|*Signature*
^h|*Meaning*

h|*0..1*
|*packages*: `xref:{class_hash}[Hash]<xref:{class_string}[String],xref:{class_bmm_package}[BMM_PACKAGE]>`
a|Child packages; keys all in upper case for guaranteed matching.

h|*Functions*
^h|*Signature*
^h|*Meaning*

h|*0..1*
|*package_at_path* ( +
a_path: `xref:{class_string}[String][1]` +
): `xref:{class_bmm_package}[BMM_PACKAGE]`
a|Package at the path `_a_path_`.

h|*1..1*
|*do_recursive_packages* ( +
action: `xref:{class_procedure}[PROCEDURE][1]` +
): `void`
a|Recursively execute `_action_`, which is a procedure taking a `BMM_PACKAGE` argument, on all members of packages.

h|*1..1*
|*has_package_path* ( +
a_path: `xref:{class_string}[String][1]` +
): `xref:{class_boolean}[Boolean]`
a|True if there is a package at the path `_a_path_`; paths are delimited with delimiter `_Package_name_delimiter_`.
|===
